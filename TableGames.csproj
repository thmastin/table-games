<Project Sdk="Godot.NET.Sdk/4.3.0">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <EnableDynamicLoading>true</EnableDynamicLoading>
    <Nullable>enable</Nullable>
    <RootNamespace>TableGames</RootNamespace>
    <AssemblyName>TableGames</AssemblyName>
  </PropertyGroup>

  <ItemGroup>
    <!-- Exclude the tests project from the main assembly -->
    <Compile Remove="tests/**" />
  </ItemGroup>

</Project>
